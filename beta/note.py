import streamlit as st

# Multiline text to display
multiline_text = """
This is a multiline text.

It can have multiple lines.

You can format it with line breaks and whitespace as needed.


In this code:

We define a multiline text using triple quotes  You can include line breaks and format the text as needed.

We use st.write() to display the multiline text. Streamlit will render the text as-is, preserving the line breaks and formatting.

When you run this code, Streamlit will display the multiline text in the app.



"""

# Display the multiline text
st.write(multiline_text)



