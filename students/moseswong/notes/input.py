#Input statement is used to ask the user a question and get their answer /response
#the input statement needs a variable to store the data

name = input('Please enter your name: ')

age = input('Enter your age: ')

print(name,'is',age,'years old')
